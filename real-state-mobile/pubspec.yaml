name: real_state_mobile
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.2.7
  dio: ^5.7.0
  dartz: ^0.10.1
  flutter_secure_storage: ^9.2.4
  intl: ^0.19.0
  json_annotation: ^4.5.0
  freezed_annotation: ^2.4.4

  # Recomendado para feature futura
  # isar: ^3.1.0+1
  # cached_network_image: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.12
  freezed: ^2.4.4
  json_serializable: ^6.8.0
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
